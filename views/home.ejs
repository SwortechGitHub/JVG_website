<link rel="preload" href="/public/files/images/top_school.webp" as="image">
<style>
  aside {
    display: none;
  }

  main {
    padding: 0;
    overflow-x: hidden;
  }

  .banner {
    z-index: 0;
    padding: 0;
    width: 100vw;
    height: calc(100vh - var(--height-nav));
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
  }

  .banner img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    position: absolute;
    right: 0;
  }

  .text-container {
    background-color: rgba(0, 0, 0, 0.7);
    backdrop-filter: blur(1.5px);
    border-radius: 10px;
    width: 50%;
    padding: 20px;
    box-sizing: border-box;
    position: relative;
    color: var(--color-white);
  }

  .banner h1 {
    font-size: 2.5rem;
    margin: 0 0 20px;
  }

  .banner p {
    font-size: 1.2rem;
    margin: 0 0 20px;
  }

  button {
    padding: 10px 20px;
    font-size: 1rem;
    cursor: pointer;
    background-color: var(--color-brand);
    color: var(--color-white);
    border: 2px solid var(--color-brand);
    border-radius: 5px;
  }

  button:hover {
    background-color: var(--color-white);
    color: var(--color-brand);
  }

  @media (max-width: 700px) {
    .banner {
      flex-direction: column;
      justify-content: center;
    }

    .text-container {
      width: 100%;
      background-color: rgba(0, 0, 0, 0.7);
      text-align: center;
      padding: 15px;
    }

    .banner h1 {
      font-size: 1.5rem;
    }

    .banner p {
      font-size: 0.9rem;
    }

    button {
      width: 100%;
      padding: 10px 0;
      font-size: 1rem;
    }
  }
</style>
<div class="banner">
  <img src="/public/files/images/top_school.webp" alt="Top School">
  <div class="text-container">
    <h1>Jauda Vērtības Ģimeniskums</h1>
    <p>Moderna skola ar vairākiem aizraujošiem pasākumiem, kur skolēni piedalās, parādot savus talantus un prasmi komunicēt.</p>
    <button>Pievienojies!</button>
  </div>
</div>